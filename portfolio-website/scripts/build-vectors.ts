// scripts/build-vectors.ts
// Prebuild: chunk every MDX file in content/, generate embeddings, and write
// them into the project-scoped and global pgvector tables in Supabase.
// See TECH-STACK.md §11, and Open Questions in §15 for the chunking strategy.

// TODO: implement chunking, embedding, and Supabase upserts.

process.exit(0);
