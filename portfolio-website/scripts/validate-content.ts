// scripts/validate-content.ts
// Prebuild gate: validate every content/{track}/{slug}/project.json against
// the ProjectMetadata zod schema. Exits non-zero to fail CI on bad content.
// See TECH-STACK.md §11.

// TODO: implement the zod-based validator.

process.exit(0);
