// scripts/build-index.ts
// Prebuild: walk content/{track}/{slug}/, validate project.json files, and
// write content/_manifest.json consumed at runtime by lib/content.ts.
// See TECH-STACK.md §11.

// TODO: implement the walker + validator + manifest writer.

process.exit(0);
