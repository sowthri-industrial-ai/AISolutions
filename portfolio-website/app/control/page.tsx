// app/control/page.tsx
// Control plane welcome workspace — placeholder.
// TECH-STACK.md §6 (welcome state, before a project is selected).

export default function ControlWelcomePage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-16">
      <h1 className="text-2xl font-medium">Control plane</h1>
      <p className="mt-4 text-muted-foreground">
        Welcome workspace placeholder. Pick a project from the sidebar to
        open its workspace.
      </p>
    </main>
  );
}
