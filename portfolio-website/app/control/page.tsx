// /control — welcome state shown before a project is selected.
// Server component — the welcome workspace is static.

import { WelcomeWorkspace } from "@/components/workspace/welcome-workspace";

export default function ControlWelcomePage() {
  return <WelcomeWorkspace />;
}
