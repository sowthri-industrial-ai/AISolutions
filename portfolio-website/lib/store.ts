// lib/store.ts
// Zustand store for control-plane state (TECH-STACK.md §3).
// Persists across project switches: model selection, temperature, panel state,
// session history. Active project + tab live in the URL.

// TODO: implement the store with `create()` from zustand, including the
// model/temperature/observabilityPanelOpen/sessionHistory slices.
// Kept as an empty module for now so consumers can be wired without
// committing to the final shape.

export {};
