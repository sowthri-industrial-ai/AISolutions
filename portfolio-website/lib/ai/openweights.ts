// lib/ai/openweights.ts
// Together AI (open-weights) provider wrapper for the model picker
// (TECH-STACK.md §3, §8). The actual installed package is
// `@ai-sdk/togetherai` — the spec's `@ai-sdk/together` is not published.

// TODO: export a configured togetherai() instance and helpers for the
// model registry. Kept as an empty module so route handlers can import
// without committing to the shape.

export {};
