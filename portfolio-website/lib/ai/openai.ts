// lib/ai/openai.ts
// OpenAI provider wrapper for the model picker (TECH-STACK.md §3, §8).

// TODO: export a configured openai() instance and helpers for the
// model registry. Kept as an empty module so route handlers can import
// without committing to the shape.

export {};
